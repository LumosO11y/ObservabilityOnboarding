1. What is Instrumentation? Explain the concept and why is it important in making your application "observable".
A: Instumentation refers to the specific code that generates or collects data about your application's runtime behavior.
   It it important to make your application "observable" because runtime behavior at production scale is a mistery to many
   developers, therefore Instrumentation gives developers the ability to observe the system behavior throught all egde cases,
 
2. What are the different ways of "instrumenting" your application. Can you use these methods at all times? Explain. Explain how each of them work in general and specifically in OpenTelemetry.
A: the diffrent ways of instumanting are:
	* Code-based solutions: Allow you to get deeper insight and rich telemetry from your application itself. 
			       They let you use the OpenTelemetry API to generate telemetry from your application, 
			       Which acts as an essential complement to the telemetry generated by zero-code solutions.
			       The way it code base instrumenation is implemented in open-telemetry is:
					* Import the OpenTelemetry API and SDK 
					* Configure the OpenTelemetry API
					* Configure the OpenTelemetry SDK
					* Create Telemetry Data
					* Export Data

	* Zero-code solutions: They provide rich telemetry from libraries you use and/or the environment your application runs in. Another way to think of it is that they provide information about what’s happening at the edges of your application.
				The way it zero code instrumenation is implemented in open-telemetry is Zero-code instrumentation adds the OpenTelemetry API and SDK capabilities to your application typically as an agent or agent-like installation. 
				The specific mechanisms involved may differ by language, ranging from bytecode manipulation, monkey patching, or eBPF to inject calls to the OpenTelemetry API and SDK into your application. 
  And you can combin zero code and code based instrumntation, it is called Hybrid instrumentation.

3. What doesn't Zero-Code instrumentation allow that Code-Based does?
A: with zero code instrumentation you cant:
	* name spans
	* instrumente logic of insernal code and not external libraries

4. Explain the different use-cases where you should do Zero-Code instrumentation and those where you should use Code-based instrumentation.
A: The main diffrence between use-cases of zero code and code base instrumantation is the code base acceability, if you have to observe the a system that you dont have
   Access to you must use zero-code instrumantation. Other use-cases of zero-code are legacy systems and third parties libraries.
   For code based instrumenations, you would use when the you want to cover the entier buissnesss logic and not just generic requests.

Skila:
Bytecode Instrumentation/Injection: Common in Java, the agent modifies the bytecode of classes as they are loaded into the JVM, inserting monitoring probes without touching source code.
Monkey Patching: Frequently used in dynamic languages like Python and Node.js, this technique replaces functions or methods at runtime with wrapper functions that include instrumentation logic.
Language Runtime APIs: Utilizing built-in capabilities of the runtime environment (e.g., JVM Tool Interface, Python sys.settrace) to intercept method calls and execution.
Compile-Time/Link-Time Instrumentation: Modifying code during the build process, such as parsing source code into an Abstract Syntax Tree (AST) to inject instrumentation before final binary generation, often used in Go.
eBPF (extended Berkeley Packet Filter): A modern, high-performance technique that attaches probes to the operating system kernel or user applications, enabling observation without modifying the application binary or using library-specific agents.

Skila #2:
eBPF - Linux kernel technology enabling engineers to build programs that run securely in kernel space. By providing safe access to the innermost workings of the operating system, eBPF lets developers tackle a wide range of challenges related to networking, observability, and security.
	https://ebpf.io/what-is-ebpf/
